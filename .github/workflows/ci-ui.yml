name: CI (UI)

on:
  push:
    branches: [main]
    paths:
      - features/ui/**
      - src/ui/**
  pull_request:
    branches: [main]
    paths:
      - features/ui/**
      - src/ui/**

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v6
      - name: 🚚 Install mise-en-place
        uses: jdx/mise-action@v4
        with:
          experimental: true
      - name: 🕵️ Run lint
        run: |
          mise run //features:lint
          mise run //src/ui:lint

  test-spec:
    name: Test specifications
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v6
      - name: 🚚 Install mise-en-place
        uses: jdx/mise-action@v4
        with:
          experimental: true
      - name: 🥒 Run specification tests
        run: mise run //src/ui:test-spec

